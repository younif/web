思路：
主线程接收的新连接，然后按照RR直接将文件描述符分配给工作线程，工作线程监听已连接套接字描述符，在解析方法后发送完文件直接关闭。
特点：设计了一个时间轮来关闭过长未完成的请求、僵尸连接。

取舍点：
没有给资源作封装，抽象层很薄，不会很长的调用栈，代价是抽象不够，后期开发会比较困难。
比如文件描述符本身只是数字，容易被重用，无法区分新旧连接导致串话。
最好使用资源类包装，将其实例化成资源类来使用，防止串化。

需要改进：
没有使用静态检查来保证数据的线程安全。在项目复杂之后难以保证安全性。
